# Ideas backlog

Pending features to batch into the next code push (so we don't trigger a
Vercel build per idea). This file is intentionally NOT pushed on its own —
it rides along when real code changes land.

## Pending

_(empty)_

## Done (on dev, awaiting push)

- [x] **% плюсовых сессий** — в правом сайдбаре + в мобильной strip; title-тултип "N из M"; без цветового кодирования (нейтральный)
- [x] **Среднее кол-во турниров за сессию** — "МТТ / сессия", округлено, и в правый сайдбар и в мобильную strip
- [x] **Фильтры по датам на графике марафона** — тогглы Неделя/Месяц/Всё время в заголовке, localStorage persist, fallback на all при <2 точек, cumMTT нормализуется так что первая видимая точка в x=0
- [x] **Убрано "день #N" с графика** — заголовок всегда показывает кол-во сессий
- [x] **Чистая пропорциональность точек по МТТ** — убран minGap=20, который искусственно раздвигал сессии с близким кол-вом турниров
- [x] **Избранные — по автору, а не посту** — клик ⭐ теперь добавляет автора целиком; посты избранных авторов обходят фильтры по лайкам/репе везде (лента, топ, темы). Хранится в `rpt_fav_authors` (старый `rpt_favs` забыт)
- [x] **Игнор — 30+ лайков показываются как свёрнутый плейсхолдер** — в ленте и темах пост игнорируемого автора с ≥30 лайков рендерится как "🚫 Пост от X (в игноре) · нажмите, чтобы посмотреть", клик раскрывает; кнопка "Вернуть" убирает автора из игнора
- [x] **Темы — добавлены "Хайлайты" и "Авторы"** — хайлайты = не-Ромео посты с ≥10 лайков; Авторы = топ-контрибьюторы с развёртыванием их постов; секция "Дебаты" переименована в "Длинные посты"; расширен TAG_RULES (деньги/румы/критика/цель)
- [x] **Детект дубликатов сессий** — `dedupBrHistory()` сливает соседние записи с одинаковым `totalTournaments` (одна сессия, зарепорченная в двух состояниях — как 22-23 марта с totalMTT=2166). Применяется сразу при загрузке meta, поэтому и stats, и чарт, и анимация баланса видят уже склеенную историю. sessionResult пересчитывается на полный диапазон, мерж-ids сохраняются в `_mergedFrom`
- [x] **Красивая анимация банкролла + помедленнее** — 4.2s вместо 2.2s, Roboto Mono + tabular-nums (нет дёргания цифр), лёгкое свечение под цвет (зелёный/красный), размер 22px (19px на мобиле)
- [x] **Индикатор свежести скрапера в футере** — показывает "X мин/часов назад" из `meta.lastUpdated`, зелёный кружок <15мин, жёлтый <60мин, красный >60мин; полный timestamp в title
- [x] **Темп "МТТ до $10M" теперь учитывает фильтр на графике** — период (неделя/месяц/всё время) поднят с MarathonChart на App-уровень; при week/month берём профит и МТТ только за выбранный отрезок и считаем новый $/МТТ; лейбл меняется на "темпом недели/месяца/текущим темпом"
- [x] **v1.5 + changelog** — bump версии + запись в футере простым языком
- [x] **Анимация смены темпа + показ $/МТТ** — новый хук `useTweenValue` (помнит предыдущее значение, плавно едет к новому, не сбрасывается в 0), компонент `TempoValue` с коротким жёлтым pulse при смене; рядом с лейблом "МТТ темпом..." теперь мелким шрифтом показано "≈ $X/МТТ" для выбранного периода
- [x] **Темп не ломает layout + формат 25k$** — темп и $/МТТ теперь лежат рядом в `.mps-value-row`, лейбл "текущим темпом" больше не переполняется; `$/МТТ` в пропорции к темпу (~9px против 13px), округление до целого или .5; везде формат денег `X$` вместо `$X` (fmtBR/fmtNum/fmtExact/fk/fkAbs)
- [x] **Одинаковый размер hero-статов** — все `.hstat-value` теперь Roboto Mono 22px (19px моб), tabular-nums; профит, день, МТТ выровнены с банкроллом
- [x] **Период-aware session stats** — МТТ/сессия и % плюсовых пересчитываются по фильтру графика (неделя/месяц), со звёздочкой и сноской "* с учётом фильтра на графике"
- [x] **Фикс графика — последняя точка показывала 5004 вместо 5171** — причина: нормализация `cumMTT` вычитала первую сессию (167 МТТ) как base, и это значение использовалось и для X-позиции, и для display-лейблов. Разделил на `cumMTT` (абсолют, лейблы) и `cumMTTX` (нормализован, X)
- [x] **Auto fit-to-screen** — `document.documentElement.style.zoom` подстраивается под ширину окна (0.85–1.25, fit to 1500px). rAF-throttle на resize, bail-out когда значение не изменилось — без лагов
- [x] **Прогресс-бар выравнен с правым виджетом** — `align-items:stretch` на grid + `margin-top:auto` на label, чтобы track прижался к нижней границе side-виджета
- [x] **Лейбл "прогресс к $10M" стал читаемым** — 11px uppercase вместо 10px dim, процент 13px белым, ближе к самому бару
- [x] **Футер — локальное время вместо "час назад"** — "сегодня в 19:47" / "12.04 в 14:03" с точностью до минуты
- [x] **Фикс "Стейкинг/Доли" таб-пилл** — `whiteSpace:nowrap`+`flexShrink:0`, больше не ломается на мобиле
- [x] **Рефакторинг** — фикс утечек rAF в `useAnimatedCounter`/`useAnimatedPath` (cleanup не вызывался), удалён неиспользуемый `day` prop у MarathonChart, три места `/romeopro/i` inline → `ROMEO_RE`
- [x] **Анимация банкролла с последнего виденного значения** — `rpt_last_seen_br` в localStorage, на маунте captured один раз. `brPath` обрезается до ближайшего вейпоинта к lastSeen → замена "проигрывать марафон с нуля" на "показать что изменилось с прошлого визита". Duration delta-aware: <0.5% → 5.2s (драматично), <3% → 4s, <10% → 3s, ≥10% → 2.2s быстрый скраб. Запись в localStorage через setTimeout(duration+250ms)
